---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-cert
  namespace: traefik
spec:
  secretName: traefik-cert
  issuerRef:
    kind: ClusterIssuer
    name: cloudflare-issuer
  dnsNames:
  - '*.k3s.hackbox.cc'
  - 'k3s.hackbox.cc'

